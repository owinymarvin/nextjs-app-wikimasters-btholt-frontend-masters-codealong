//terminal - starship
curl -sS https://starship.rs/install.sh | sh
eval "$(starship init zsh)" // zshrc


bun run lint // biome checks 
bun run lint -- --fix // fix issues with biome
bun run lint -- --fix --unsafe // fix issues with biome
bun biome format // runs the biome formatter similar to prettier
bun run format // does the same as the above. 
bun run typecheck // added to pkg.json "typecheck": "tsc --noEmit"


// tailwind. 
learn tailwind, ensure that the variables are re-usable. Learn once, write everywhere
make variants for the different buttons, etc. 
CVA - class Variants Authority, in tailwind helps u manage this. 

// Packages
bun shadcn@latest init // Slate
bun shadcn@latest add @shadcn/navigation-menu
bun shadcn@latest add @shadcn/button
bun run lint -- --fix --unsafe // fix issues with biome
bun shadcn@latest add @shadcn/card  , badge, input 


// Stack Auth. 
npx @stackframe/init-stack@latest --no-browser // Register and sign up. 
https://fullstack-v4.holt.courses/lessons/auth/signin-and-signup // proecedure to setup stack auth and the neon database string 
 
// Drizzle ORM. 
bun install drizzle-orm @neondatabase/serverless dotenv
bun install -D drizzle-kit drizzle-seed // creates and manages migrations for you, seed generates seed or initial data. 
bun drizzle-kit generate
bun drizzle-kit migrate